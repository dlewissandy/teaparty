Make 4 surgical edits to one Python file using the Edit tool. Read the file first, then make each edit in order.

FILE: /Users/darrell/git/teaparty/projects/POC/.worktrees/session-20260308-203749-amg0/projects/POC/scripts/memory_indexer.py

STEP 1: Read the file.

STEP 2: EDIT 1 — Replace source filtering in main() (around line 752-753).

OLD:
    # Filter to existing, non-empty source files
    sources = [s for s in args.sources if Path(s).is_file() and Path(s).stat().st_size > 0]

NEW:
    # Filter sources: expand directory paths to contained .md files; keep non-empty files
    sources = []
    for s in args.sources:
        p = Path(s)
        if p.is_dir():
            for f in sorted(p.glob('*.md')):
                if f.is_file() and f.stat().st_size > 0:
                    sources.append(str(f))
        elif p.is_file() and p.stat().st_size > 0:
            sources.append(s)

STEP 3: EDIT 2 — Add --scope-base-dir argparse argument after --retrieved-ids (around line 744-747).

OLD:
    parser.add_argument("--retrieved-ids", default="", dest="retrieved_ids",
                        help="Path to write retrieved entry IDs for reinforcement tracking")
    args = parser.parse_args()

NEW:
    parser.add_argument("--retrieved-ids", default="", dest="retrieved_ids",
                        help="Path to write retrieved entry IDs for reinforcement tracking")
    parser.add_argument("--scope-base-dir", default="", dest="scope_base_dir",
                        help="Project base dir for scope multiplier (team > project > global)")
    args = parser.parse_args()

STEP 4: EDIT 3 — Insert scope weighting functions before the Formatting section (around line 719-721).

OLD:
# -- Formatting ----------------------------------------------------------------

def format_chunks(results: list[tuple[str, str, float]]) -> str:

NEW:
# -- Scope weighting -----------------------------------------------------------

SCOPE_MULTIPLIERS = {
    'team': 1.5,
    'project': 1.2,
    'global': 1.0,
}


def classify_scope(source_path: str, base_project_dir: str) -> str:
    """Classify source scope: 'team' (in .sessions/), 'project', or 'global'."""
    if not base_project_dir:
        return 'global'
    try:
        p = Path(source_path).resolve()
        base = Path(base_project_dir).resolve()
        rel = p.relative_to(base)
        if '.sessions' in rel.parts:
            return 'team'
        return 'project'
    except ValueError:
        return 'global'


def apply_scope_multipliers(
    results: list[tuple[str, str, float]],
    base_project_dir: str,
) -> list[tuple[str, str, float]]:
    """Multiply scores by scope-level weight: team x1.5, project x1.2, global x1.0."""
    if not base_project_dir:
        return results
    return [
        (source, content, score * SCOPE_MULTIPLIERS.get(classify_scope(source, base_project_dir), 1.0))
        for source, content, score in results
    ]


# -- Formatting ----------------------------------------------------------------

def format_chunks(results: list[tuple[str, str, float]]) -> str:

IMPORTANT for EDIT 3: The section header lines use unicode box-drawing dashes. Read the file carefully and match the exact characters used in the "# ── Formatting ──" header line. If those dashes are unicode (U+2500), match them exactly. If it uses regular hyphens, match those. Either way, the new "# -- Scope weighting --" header should use regular hyphens as shown above.

STEP 5: EDIT 4 — Call apply_scope_multipliers in main() (around line 822-825).

OLD:
    # Apply prominence weighting (replaces apply_temporal_decay — removes evergreen exemption)
    results = apply_prominence_weights(results, conn)
    results.sort(key=lambda x: x[2], reverse=True)
    results = mmr_rerank(results, top_k=args.top_k)

NEW:
    # Apply prominence weighting (replaces apply_temporal_decay — removes evergreen exemption)
    results = apply_prominence_weights(results, conn)
    if args.scope_base_dir:
        results = apply_scope_multipliers(results, args.scope_base_dir)
    results.sort(key=lambda x: x[2], reverse=True)
    results = mmr_rerank(results, top_k=args.top_k)

STEP 6: Verify by running:
python3 /Users/darrell/git/teaparty/projects/POC/.worktrees/session-20260308-203749-amg0/projects/POC/scripts/memory_indexer.py --help 2>&1 | grep scope-base-dir

Report the exact grep output (it should show the --scope-base-dir argument line).
