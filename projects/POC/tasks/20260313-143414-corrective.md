

---
id: 470dd19e-242a-41cf-98e4-d60bbeb6573f
type: procedural
domain: task
importance: 0.8
phase: unknown
status: active
reinforcement_count: 0
last_reinforced: '2026-03-13'
created_at: '2026-03-13'
---
## [2026-03-13] Delegation Timeout Pattern
**Error Pattern:** Attempted to delegate `docs/index.md` rewrite to `@writing-liaison` agent via SendMessage. Agent did not respond or complete work after first dispatch. User re-dispatched with more explicit specs, still no response. After two failed dispatch attempts and wait cycles, user abandoned the delegation model and directly executed the task with the Write tool, which succeeded immediately.

**Recovery:** Bypassed delegation and used direct Write tool execution. Task completed successfully in a single operation.

**Root Cause:** Named agent (`writing-liaison`) either doesn't exist, isn't available, or isn't configured for documentation tasks. The dispatch model created a blocking wait loop rather than failing fast.

**Prevention:** For future sessions, set a strict timeout on named agent dispatches (1-2 checks max) before switching to direct execution. If a specific agent dispatch is critical, confirm it exists and is available before relying on it. For text/content work that can be done directly, prefer direct execution over dispatch to avoid unnecessary coordination overhead.
