Execute Pass 1 (Information Architecture) of the TeaParty docs rewrite. All files are in the worktree at /Users/darrell/git/teaparty/projects/POC/.worktrees/session-112714--the-docs-folder-contains-the-p/. Use this as the root. Docs are at docs/ relative to that root.

This is a five-pass rewrite of the TeaParty public-facing docs. Pass 1 fixes the document hierarchy so structure matches what an external reader needs. The goal is NOT to rewrite prose -- just restructure: trim redundancy, consolidate open questions, move implementation-depth content, and make each file cover exactly one thing at the right level of abstraction.

No content should be deleted -- it moves or merges. "Earn every sentence" applies to structure, not to individual phrases yet (that is Pass 4).

ACTION 1: Trim docs/ARCHITECTURE.md (~289 to ~180 lines)

Read the file first. Then:
- Remove the "Conversation Kinds" routing table section. Replace it with one sentence: "Agent messages are routed by conversation kind; the full routing model is in [Agent Dispatch](agent-dispatch.md)."
- Remove the "Feedback Bubble-Up Model" subsection (it is duplicated verbatim in engagements-and-partnerships.md, which owns that topic). Replace with one sentence pointing there.
- Remove the detailed engagement lifecycle states (also fully covered in engagements-and-partnerships.md). Replace with one sentence pointing there.
- Remove the "Cycle Prevention" detail section. Replace with one sentence pointing to engagements-and-partnerships.md.
- Remove the entire "Open Questions" section (questions move to research-directions.md in Action 2).
- Keep: Corporate Hierarchy (with ASCII diagram), Work Hierarchy (with ASCII diagram), Human Interaction Model (high-level), Agent Model (types table), Workspace and Filestore Model (high-level), Technology Stack, and the "Further Reading" section.
- The file should end up around 170-185 lines.

ACTION 2: Create docs/research-directions.md

Create a new file that consolidates all open questions currently scattered across docs. Source them from:
- ARCHITECTURE.md -- 5 questions (before you remove them)
- engagements-and-partnerships.md -- 3 questions (note: 2 of these duplicate ARCHITECTURE.md questions; include them only once)
- intent-engineering.md -- 4 questions
- sandbox-design.md -- 6 questions
- learning-system.md -- 5 questions

Format: Group by theme, not by source file. Each question should include a brief label indicating which area it relates to. Introduce the file with 2-3 sentences explaining that these are active research and design questions, not gaps -- they are where interesting work is.

After creating this file, edit each of the 5 source files to remove their "Open Questions" section body and replace it with a single line: "Open research questions for this area are collected in [Research Directions](research-directions.md)."

ACTION 3: Create a condensed docs/poc-architecture.md and move the full version

The current docs/poc-architecture.md is 527 lines -- too deep for an external reader. It is the authoritative implementation reference and should live in projects/POC/docs/.

Steps:
1. Copy the full docs/poc-architecture.md content to projects/POC/docs/poc-architecture.md (that directory already exists and has other files; this is a new file).
2. Replace docs/poc-architecture.md with a condensed version (~100-120 lines) aimed at an external reader who wants to understand what the POC IS, not how to run it. It should cover:
   - What the POC demonstrates (one paragraph)
   - The two-level hierarchy: uber team + subteams, what each does
   - The CfA state machine: three phases, what each covers
   - Git worktree isolation: why it matters
   - Learning extraction: what it does and when it runs
   - A "For contributors" note at the bottom pointing to projects/POC/docs/poc-architecture.md for the full implementation reference.

ACTION 4: Narrow docs/folder-structure.md

Read the file. It currently mixes POC-specific directory layout with platform-aspirational hierarchy content. Remove the platform hierarchy content (the corporate hierarchy ASCII diagram at the top and any description of the multi-tenant org structure). Keep only:
- The actual POC directory layout on disk
- Memory hierarchy paths (dispatch to team to session to project to global)
- Worktree isolation model
- Dogfooding setup

Add a note at the end: "For the platform's virtual file tree (workgroups, engagements, scoped files), see [File Layout](file-layout.md)."

ACTION 5: Condense docs/agent-dispatch.md

The current file (246 lines) contains routing tables and internal method names (TeamSessionManager, team_bridge.py, etc.) that are platform implementation details. An external reader needs to understand what dispatch IS, not the internal plumbing.

Trim to ~80-100 lines covering:
- What agent dispatch is conceptually
- The liaison pattern: how an uber-team agent delegates to a subteam
- High-level routing: how message kind determines which team receives a message
- The dispatch flow at a conceptual level (no method names, no class names, no routing tables with enum values)

Content that gets cut here does not need to be preserved elsewhere -- it describes unbuilt platform internals, not the POC.

CONSTRAINTS

- Do not rewrite prose style -- that is Pass 3's job. Edit structure only: remove sections, merge content, add pointers.
- Do not touch docs/research/ (except noting that it exists).
- Do not touch any other docs files not listed above.
- No content invented -- everything in the condensed poc-architecture.md must come from the existing 527-line file.
- The 5-question removal from ARCHITECTURE.md must happen BEFORE or AS PART OF writing research-directions.md so no questions are lost.

DELIVERABLES CHECKLIST

When done, confirm:
- docs/ARCHITECTURE.md -- trimmed to ~180 lines, 4 sections removed/summarized, Open Questions removed
- docs/research-directions.md -- created with ~21 deduplicated questions grouped by theme
- docs/poc-architecture.md -- condensed to ~100-120 lines for external readers
- projects/POC/docs/poc-architecture.md -- full 527-line version moved here
- docs/folder-structure.md -- platform content removed, pointer to file-layout.md added
- docs/agent-dispatch.md -- trimmed to ~80-100 lines, implementation internals removed
- All 5 source files have their Open Questions sections replaced with a pointer line

Report what files were changed and a one-line description of what changed in each.
