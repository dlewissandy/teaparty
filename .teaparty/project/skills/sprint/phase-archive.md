# Phase 6: Archive

Sprint is over. Close out the local cache.

`Delegate(scrum-master, "Archive this sprint.", skill='archive-sprint')`. `CloseConversation` on the reply.

Read the reply. `archive-sprint` warns on Approved-still-open items but does not block. If there are leftover Approved items, summarize them in your DELIVER text so the human can decide whether to roll them forward to the next sprint.

DELIVER. End your turn with a one-line summary: which milestone, how many issues finished, how many leftover, and the cache archive path.

Halt.
