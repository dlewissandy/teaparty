Make the following two surgical changes to /Users/darrell/git/teaparty/poc/projects/POC/scripts/summarize_session.py. Read the file first to get exact line numbers and surrounding context before making edits. Do not rewrite the file. Make surgical edits only. Verify the final result looks correct.

CHANGE 1: Add a silence instruction to four scope prompts in the PROMPTS dictionary.

In the PROMPTS dictionary, add the following sentence at the end of the instructions block (before the format block) for EACH of these four scopes: team, team-rollup, session, and project.

Sentence to add:
If no learnings meet the quality bar, output nothing. Do not explain why or state that there are no learnings. Silence is correct.

For the team scope: add it after the last bullet point that reads "- About HOW to work effectively..." and before the "Format each learning as:" line.

For the team-rollup scope: add it after the "Deduplicate: ..." line and before the "Format each learning as:" line.

For the session scope: add it after the EXCLUDE block (after the line "- Domain-specific content...") and before the "Format each learning as:" line.

For the project scope: add it after the "Deduplicate with any existing..." line and before the "Format each learning as:" line.

CHANGE 2: Add meta-commentary filter in summarize() function.

In the summarize() function, locate the section after "learnings = result.stdout.strip()" and after the existing check for "Error: Reached max turns". Add the following block immediately after the existing early-return checks, before the "# Append to output file" section:

    # Reject outputs that contain no properly-formatted entries.
    # A valid learning entry must start with "## [". Pure prose output
    # (meta-commentary, explanations of why nothing qualifies) must be
    # discarded and must never be persisted to memory files.
    if learnings and not any(line.strip().startswith("## [") for line in learnings.splitlines()):
        print(f"[summarize] No formatted entries found in output (meta-commentary?), skipping.", file=sys.stderr)
        return 0
